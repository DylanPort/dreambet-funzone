
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Configure asset optimization
[build.processing]
  skip_processing = false

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
